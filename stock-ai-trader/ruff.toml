# Ruff configuration for stock-ai-trader
line-length = 120
target-version = "py311"

[lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E402",  # Module-level import not at top (sys.path setup pattern)
    "E501",  # Line too long (formatter handles this)
]
